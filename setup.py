from setuptools import setup

setup(
    name='campy',
    version='0.1.0',
    install_requires=['numpy',
                      'imageio-ffmpeg',
                      'scikit-image',
                      'matplotlib',
                      'pypylon',
                      'torch']
)